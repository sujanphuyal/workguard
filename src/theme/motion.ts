/** Motion presets intentionally disabled — spring enter animations caused layout instability on list re-renders. */

export const bootIn = undefined;
export const bootInSubtle = undefined;
export const bootInUp = undefined;

export function staggeredBoot(_index: number) {
  return undefined;
}
